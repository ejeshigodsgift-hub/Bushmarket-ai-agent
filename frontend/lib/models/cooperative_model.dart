ListView(
  children: [
    Text("Active Cooperatives"),
    ElevatedButton(
      onPressed: () {
        // call CoopService.createCoop()
      },
      child: Text("Create Cooperative"),
    ),
  ],
);